{
  "name": "@usepromise/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Promise MCP server over the Promise platform API",
  "scripts": {
    "build": "tsc --noEmit",
    "tools": "node --import tsx src/index.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
